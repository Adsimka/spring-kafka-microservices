server:
  port: 8090

spring:
  application:
    name: consumer
  kafka:
    consumer:
      bootstrap-servers: localhost:9092,localhost:9094
      group-id: product-created-events
      properties:
        spring:
          json:
            trusted:
              packages: "*"